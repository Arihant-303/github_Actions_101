name: My First workflow
on: [push]
jobs:
    First_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v6

            - name: Wecome message
              run: |
                  echo "Hello, We are learning Github Actions"
                  ls
                  cat README.md
