name: My First workflow
on: [push]
jobs:
    First_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v6
              
            - name: Wecome message
              run: echo "Hello, We are learning Github Actions"

            - name: List all the files
              run: ls

            - name: Read the file
              run: cat README.md