name: My Second workflow
on: [push]
jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v6

            - name: Executing Shell script
              run: 
                chmod +x ./Ascii_shell.sh
                ./Ascii_shell.sh

